# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-pos-change-this-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=db_pos
DB_USER=root
DB_PASS=your_password

# Celery / Event Bus (RabbitMQ)
EVENT_BUS_URL=amqp://guest:guest@localhost:5672
CELERY_BROKER_URL=amqp://guest:guest@localhost:5672
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Redis (opcional, para caché y Channels)
REDIS_URL=redis://localhost:6379/1
CHANNEL_LAYERS_BACKEND=redis://localhost:6379/2

# JWT Settings
JWT_SECRET_KEY=your-jwt-secret-key
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60

# URLs de servicios externos
OPERATIONS_SERVICE_URL=http://localhost:8001
